services:
  postgres:
    image: postgres:17-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: whisker_test
      POSTGRES_USER: whisker
      POSTGRES_PASSWORD: whisker
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
